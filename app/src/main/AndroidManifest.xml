<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mybooks.mybooks">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".app_pref.MyBooks"
        android:allowBackup="false"
        android:icon="@drawable/shopyclubicon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.LoginActivity"
            android:windowSoftInputMode="stateHidden">

        </activity>

        <activity
            android:name=".activities.HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".activities.BooksListPageNew"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".activities.AddressActivity" />
        <activity android:name=".activities.BlogActivity" />
        <activity android:name=".activities.PaymentPageActivity" />
        <activity android:name=".activities.OrderDetailsActivity" />
        <activity android:name=".activities.MyAccountActivity" />

        <service android:name=".services.MyBooksService" />

        <activity android:name=".activities.Individual_book_details" />
        <activity android:name=".activities.MyCartNew" />
        <activity android:name=".activities.About" />

        <activity
            android:name="com.paytm.pgsdk.PaytmPGActivity"
            android:configChanges="keyboardHidden|orientation|keyboard"
            android:screenOrientation="portrait" />

        <activity android:name=".activities.OrderMainPage" />
        <activity android:name=".activities.CustomOrderMainPage" />
        <activity android:name=".activities.CustomOrderAddProduct" />
        <activity android:name=".activities.OrderCustomDetailsActivity" />
        <activity
            android:name=".activities.OrderPageActivity"
            android:exported="true" />

        <activity android:name=".activities.Login_2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>