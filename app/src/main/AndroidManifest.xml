<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mybooks.mybooks">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:name=".MyBooks"
        android:allowBackup="true"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme" />
        <activity android:name=".BooksListPageNew" />
        <activity android:name=".AddressActivity" />
        <activity android:name=".PaymentPageActivity" />
        <activity android:name=".OrderPageActivity" />
        <activity android:name=".OrderDetailsActivity" />
        <activity android:name=".CustomOrderActivity" />
        <activity android:name=".MyAccountActivity" />

        <service android:name=".MyBooksService" />

        <activity android:name=".Individual_book_details" />
        <activity android:name=".MyCartNew" />
        <activity android:name=".BlogActivity" />
        <activity android:name=".About"></activity>
        <activity
            android:name="com.paytm.pgsdk.PaytmPGActivity"
            android:screenOrientation="portrait"
            android:configChanges="keyboardHidden|orientation|keyboard"/>


    </application>

</manifest>